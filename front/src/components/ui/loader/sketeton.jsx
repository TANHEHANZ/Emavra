import React from "react";
import "./SkeletonLoader.css"; // Asegúrate de incluir el CSS correspondiente

const SkeletonLoader = () => (
  <section className="containerLoader">
    <div className="loader"></div>;
  </section>
);

export default SkeletonLoader;
